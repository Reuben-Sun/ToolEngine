#pragma once

namespace ToolEngine
{
	// TODO: store render context
	// have many pointer of manager, such as particle, texture, render setting
	// when I want to create a texture, I just need use context->textureManager->createTexture()
}